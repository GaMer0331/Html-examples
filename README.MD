<!doctype html> <html lang="en"> <head> <meta charset="utf-8"> <meta name="viewport" content="width=device-width,initial-scale=1"> <title>Main Page</title> <style> :root{ --bg1:#0f172a; --bg2:#111827; --accent1:#06b6d4; --accent2:#7c3aed; } *{box-sizing:border-box} html,body{height:100%;margin:0} body{ display:flex;align-items:center;justify-content:center; font-family:system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial; background:linear-gradient(135deg,var(--bg1),var(--bg2)); -webkit-font-smoothing:antialiased;color:#fff; } .container{display:flex;align-items:center;justify-content:center;width:100%;height:100%} a.btn{ position:relative;display:inline-block;padding:16px 40px;border-radius:999px; color:#fff;text-decoration:none;font-weight:700;font-size:18px; background:linear-gradient(90deg,var(--accent1),var(--accent2)); box-shadow:0 10px 30px rgba(2,6,23,0.6),0 2px 6px rgba(0,0,0,0.4); transition:transform .18s ease,box-shadow .18s ease,filter .18s; overflow:hidden; } a.btn::before{ content:"";position:absolute;inset:0;border-radius:999px; background:linear-gradient(90deg,rgba(255,255,255,0.06),rgba(255,255,255,0)); filter:blur(12px);opacity:0.6;pointer-events:none; } a.btn:hover{transform:translateY(-6px) scale(1.02);box-shadow:0 18px 48px rgba(2,6,23,0.7)} a.btn:active{transform:translateY(-2px) scale(.99)} a.btn:focus{outline:3px solid rgba(124,58,237,0.12);outline-offset:6px} .ripple{position:absolute;border-radius:50%;transform:scale(0);animation:ripple .6s linear;background:rgba(255,255,255,0.35);pointer-events:none} @keyframes ripple{to{transform:scale(4);opacity:0}} </style> </head> <body> <main class="container"> <!-- change href="#" to a URL or use JS to perform an action --> <a class="btn" href="#" id="mainBtn" aria-label="Main Action">Get Started</a> </main> <script> document.getElementById('mainBtn').addEventListener('click', function(e){ // ripple effect const btn = e.currentTarget; const rect = btn.getBoundingClientRect(); const ripple = document.createElement('span'); ripple.className = 'ripple'; const size = Math.max(rect.width, rect.height); ripple.style.width = ripple.style.height = size + 'px'; ripple.style.left = (e.clientX - rect.left - size/2) + 'px'; ripple.style.top = (e.clientY - rect.top - size/2) + 'px'; btn.appendChild(ripple); setTimeout(()=> ripple.remove(), 600); // prevent default for demo (remove if you want real navigation) e.preventDefault(); // place custom action here if needed }); </script> </body> </html>
